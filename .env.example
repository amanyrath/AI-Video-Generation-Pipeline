# ============================================================================
# AI Video Generation Pipeline - Environment Variables
# ============================================================================
# 
# Copy this file to .env.local and fill in your actual values
# 
#   cp .env.example .env.local
# 
# ============================================================================

# ----------------------------------------------------------------------------
# OpenRouter API (Required)
# ----------------------------------------------------------------------------
# Used for storyboard generation via GPT-4o
# 
# Get your API key from: https://openrouter.ai/keys
# Format: Should start with "sk-or-v1-" or "sk-"
OPENROUTER_API_KEY=

# ----------------------------------------------------------------------------
# Replicate API (Required)
# ----------------------------------------------------------------------------
# Used for image generation via Flux-schnell model
# 
# Get your API token from: https://replicate.com/account/api-tokens
# Format: Should start with "r8_"
REPLICATE_API_TOKEN=

# ----------------------------------------------------------------------------
# AWS S3 Configuration (Optional - For Future File Storage)
# ----------------------------------------------------------------------------
# These will be used when implementing S3 file storage for images/videos
# Currently, files are stored locally in /tmp/ (ephemeral)
# 
# Get your credentials from: https://console.aws.amazon.com/iam/
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=your-secret-access-key-here
AWS_S3_BUCKET=your-bucket-name

# ----------------------------------------------------------------------------
# Application Configuration (Optional)
# ----------------------------------------------------------------------------
# Public URL of your application
# Used for OpenRouter API referer header
# Defaults to http://localhost:3000 if not set
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================================
# Notes
# ============================================================================
# 
# 1. Required Variables:
#    - OPENROUTER_API_KEY (for storyboard generation)
#    - REPLICATE_API_TOKEN (for image generation)
# 
# 2. Optional Variables:
#    - AWS_* variables (for future S3 integration)
#    - NEXT_PUBLIC_APP_URL (defaults to localhost:3000)
# 
# 3. Testing Your Configuration:
#    - GET /api/test-keys - Verify all API keys are configured
#    - GET /api/test-openrouter - Test OpenRouter connection
#    - GET /api/test-ffmpeg - Test FFmpeg availability
# 
# 4. Security:
#    - Never commit .env.local to version control
#    - .env.local is already in .gitignore
#    - Use environment variables in production (Vercel, AWS, etc.)
# 
# ============================================================================
